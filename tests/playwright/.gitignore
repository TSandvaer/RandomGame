# Local build artifacts (downloaded HTML5 zips for local test runs)
.local-builds/
.artifact-tmp/
html5-build/

# Playwright runtime artifacts
test-results/
playwright-report/
playwright/.cache/
# JSON reporter output (PLAYWRIGHT_JSON_OUTPUT_NAME — local debugging only)
*-result.json
*-results.json

# Node
node_modules/
