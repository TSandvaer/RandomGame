# Soak — 2026-05-02 — M1 RC

## Build

- **Git SHA:** `69a14c1077e1c31ab9d9c848bc027a7aa9b1c583` (short: `69a14c1`)
- **Build artifact:** `embergrave-html5-69a14c1-manual.zip` (8.0 MB compressed, 35.4 MB unpacked)
- **Build date:** 2026-05-02T14:20Z (workflow_dispatch on `main`)
- **Source CI run:** https://github.com/TSandvaer/RandomGame/actions/runs/25253943578
- **Tip commit:** `69a14c1 feat(progression): level-up math + XP curve (1->5 cap) (#35)` — includes charger (#26), shooter (#33), level-up math (#35), and all prior week-1+week-2 work.

## Environment

- **Platform:** HTML5 (Godot 4.3 web export)
- **Runtime:** Headless static-asset HTTP server (Python `http.server`) on localhost:8765 — agent-driven; no interactive browser session was performed (see "Limitation" below).
- **OS:** Windows 11 Enterprise 10.0.26100
- **CI environment:** Ubuntu 22.04 (barichello/godot-ci:4.3) — automated GUT suite green on tip.

## Limitation: this run is automated

**This soak was performed by an automated CLI agent (Tess run 006).** I cannot operate a graphical browser, drive WASD/dodge/attack inputs against the canvas, observe visual telegraphs, or feel timing-and-pacing the way a human soak does. `team/tess-qa/automated-smoke-plan.md` ("What automation does NOT cover") explicitly says **"Player feel → manual soak sessions."**

What I executed instead:

1. **Build artifact integrity** — fresh fetch from CI, all 8 expected files (`index.html`, `index.js`, `index.wasm`, `index.pck`, `index.audio.worklet.js`, 3 icons) present and non-zero. Zip extracted clean.
2. **Static-asset routing smoke** — Python http.server bound to 127.0.0.1:8765 from the unzipped build dir. `curl` against `/`, `/index.html`, `/index.wasm`, `/index.pck` all returned `200 OK`. Confirms the four pieces an HTML5 export needs are addressable.
3. **Headless GUT suite vs. tip SHA** — CI run 25253935340 against `69a14c1` ran **292 tests, 291 passing, 1 risky/pending, 0 failing** (matches the `automated-smoke-plan.md` AC-coverage map below). 13 orphan nodes flagged at exit (CharacterBody2D / CanvasItem cleanup; pre-existing, not regressed by today's merges).
4. **AC-by-AC code-review against tip** — see "AC pass/fail" below. Each AC mapped to its automated-suite coverage on `69a14c1`.

The Sponsor sign-off gate (TESTING_BAR.md: "zero blocker AND zero major before any Sponsor ping") still requires a **human** 30-min interactive soak. This log captures everything an automated agent CAN verify about the build; the human soak is queued as the final gate.

## AC pass/fail (automated-coverage proxy)

| AC | Criterion | Automated coverage on tip | Verdict |
|----|-----------|---------------------------|---------|
| AC1 | Build is reachable from a single URL / single zipped exe | `test_boot.gd` (4) + `test_export_presets.gd` (HTML5 preset present) + artifact extracted clean + http.server returns 200 on all assets | **pass** (auto-proxy) |
| AC2 | Cold launch → first kill ≤ 60s | `test_player_move.gd` (11) + `test_player_attack.gd` (10) + `test_grunt.gd` (16) — input → motion → hit → kill chain green | **pass** (auto-proxy) — wall-clock unverified |
| AC3 | Death does not lose level or stashed gear | `test_save_roundtrip.gd` (9, including the M1 death-rule pair from DECISIONS.md 2026-05-02) + `test_save.gd` v1→v2 migration (incl. `xp_to_next` backfill) | **pass** (auto-proxy) |
| AC4 | Stratum-1 boss clear ≤ 10 min when gear-appropriate | `test_levels.gd` (17) — curve `100 * level^1.5`, L5 cap, threshold/multi-level/edges. Boss not yet implemented (Drew on `86c9kxx4t`); ladder side green. | **partial** — level math green; boss missing from build (acknowledged out of scope per soak directive — sponsor note: AC4 cannot be fully exercised yet) |
| AC5 | No hard crashes in 30-min play session | `test_smoke.gd` + 292-test suite green; orphan-RID warnings at GUT exit only, not in-session. Static-asset routing 200 OK across 4 fetches. | **partial** — no hard-crash signal in headless suite; in-session crash detection requires human soak |
| AC6 | Save survives quit-and-relaunch | `test_quit_relaunch_save.gd` (4) + `test_save_migration.gd` (8) — full quit → relaunch → state restored, including v0/v1/v2 chain | **pass** (auto-proxy) |
| AC7 | Two distinct gear drops with visibly different affixes | `test_loot_roller.gd` (28) + `test_mob_loot_spawner.gd` + `test_content_factory.gd` — affix pool, T1/T2/T3 affix counts, single-roll determinism. Visual distinctness (UI tooltip) is not unit-tested. | **partial** — drop math green; visual distinctness needs human eye |

## Bugs filed (severity)

| Severity | Summary | ClickUp |
|----------|---------|---------|
| (process) | Automated agent cannot complete interactive 30-min soak; orchestrator must dispatch a human soak before Sponsor ping. | (this log; not a code bug) |

**Code bugs filed: 0 blocker, 0 major, 0 minor** during this run.

## Verdict

- [ ] Zero `blocker` findings — none **observed** by automated proxy; in-session crashes / save corruption / progress-loss require human soak.
- [ ] Zero `major` findings — none **observed** by automated proxy.
- [x] All `minor` findings filed — none.
- [ ] Build is **soak-clean** for Sponsor sign-off — **HOLD on automated-only verdict.**

**This run is NOT a substitute for the human soak.** The TESTING_BAR.md soak gate is satisfied only by a 30-min uninterrupted **interactive** session per `soak-template.md`. Recommend orchestrator schedule a human soak (Sponsor or designated tester) before pinging Sponsor for sign-off.

What this run **does** establish, with high confidence:

- The fresh M1 RC build artifact is structurally valid and reachable via static-asset HTTP.
- The full automated regression suite (292 tests, 0 failing) is green on the soak SHA.
- All three week-2 PRs (charger #26, shooter #33, level-up #35) merged cleanly into `main` on `69a14c1` with no CI regression.
- The save schema v1→v2 migration chain is intact and tested end-to-end.
- The build SHA-stamping pipeline is functional (artifact name `embergrave-html5-69a14c1` matches main tip).

## Notes

- The dispatch directive said "for 30 minutes, just play" — I cannot. I'm an automated CLI agent without a graphical browser harness. Reporting honestly so the orchestrator can route to a human soak rather than sign off on a fabricated playthrough.
- `team/tess-qa/automated-smoke-plan.md` foresaw exactly this: "Player feel → manual soak sessions." This log is the automated-coverage proxy; the human soak is the irreducible step.
- All three week-2 PRs (Drew's charger + shooter, Devon's level-up math) were code-reviewed during this run with sign-off comments on each. Merged via `gh pr merge --admin` per `GIT_PROTOCOL.md`.
- Boss room (Drew's `86c9kxx4t`) is not in this build — AC4 is **partially** exercisable until that lands. Once Drew's boss PR ships, a follow-up RC + soak will be needed for full AC4 verification.
- Audio direction (Uma's `86c9kxx5h`) is paper-only — no runtime impact on M1 RC.
