## What + why

The S1 level-design pivot (ticket `86ca64xzb`, `art-direction.md` § "The FINAL
pivot", memory `level-design-pivot-godot-editor-sourced-tileset`): the
team-builds-the-level approach is retired. Sponsor designs S1 himself in the Godot
editor using a **sourced professional tileset** (Cainos "Pixel Art Top Down –
Basic", free, 32px) + the carried-forward PixelLab props. This PR **stands up the
paintable workflow** — it does NOT design or paint a level (that's Sponsor's job
now).

Repo is private, so the Cainos pack is committed license-clean.

## Deliverables

1. **Cainos textures** → `assets/tilesets/cainos/` (snake_cased for clean res://
   paths). Committed: grass / stone-ground / wall / props / plant / struct / shadow
   sheets. Skipped `.unitypackage` + `TX Player` per ticket. `.import` sidecars are
   gitignored (regenerated by headless Godot on first import).
2. **`resources/tilesets/cainos_s1.tres`** — TileSet built via the Godot API +
   `ResourceSaver` (`tools/build_cainos_tileset.gd`), **NOT hand-written**. This is
   load-bearing: a hand-authored corner-Wang `.tres` silently drops the per-tile
   peering bits (`procgen-pipeline.md` § "Godot autotile TERRAIN authoring", v5
   lesson) → the autotiler loses its transition tiles. 6 atlas sources; a
   `MATCH_CORNERS` terrain set with **grass** (0) + **stone_path** (1) terrains for
   grass↔cobble-path autotile blending. Stone-ground / wall / props / plant / struct
   are registered as plain paintable atlas sources (discrete tiles, not corner
   terrains — those sheets are framed slabs / bricks / objects, not alpha-cutout
   blob sets).
3. **`scenes/levels/s1_yard_authored.tscn`** — the paintable scene Sponsor opens:
   `Ground` / `StoneGround` / `Walls` `TileMapLayer`s (Cainos set assigned) + a
   `Props` container + an instanced `Player` + a runtime follow-`Camera2D`
   (`S1YardAuthored.gd`, zoom mirrors `CameraDirector.BASELINE_ZOOM` so painted
   tiles read at gameplay scale). A starter ground patch (280 cells, grass field +
   autotiled stone-path strip) is painted via `set_cells_terrain_connect` so the
   scene **renders Cainos tiles the moment it opens**. Runs on **F6** (Play Current
   Scene).
4. **`scenes/levels/s1_prop_palette.tscn`** — a copyable tray of the 5
   carried-forward PixelLab cloister props (pillar / brazier / banner / rubble /
   parchment) + a few Cainos props as `AtlasTexture` sprites. Ctrl+C a prop → Ctrl+V
   into the authoring scene's `Props` node.
5. **`team/drew-dev/s1-paint-guide.md`** — short Sponsor how-to (open, paint terrain
   with the autotile brush, place props, save, F6 to run).

## Corner-peering-bit derivation

The grass↔stone-path corner map (`tools/build_cainos_tileset.gd` `PATH_CORNERS`)
was derived empirically from `tx_tileset_grass.png` rows 4–7 by classifying each
16×16 corner-quadrant as grass vs stone via the **blue channel** (cobble blue≈114
vs olive-grass blue≈27, split at >55). Full record + re-derive snippet in
`tools/_cainos_corner_map.md`. To tune a blend: edit `PATH_CORNERS` and re-run the
builder — never hand-edit the generated `.tres`.

## Note on the prop path

The ticket/brief reference `assets/props/s1_yard/*.png`, but that directory does
not exist on `main`. The 5 carried-forward PixelLab building props actually live at
`assets/props/s1_cloister/` (the set consumed by the existing
`s1_yard_slice_chunk.tscn`: `pillar_arch`, `brazier_lit`, `banner_worn`,
`rubble_01`, `parchment_01`, plus `brazier_cold`). The palette uses the real
on-main location rather than fabricate the missing path. Flag if a dedicated
`s1_yard/` building set is expected.

## Tests

- **GUT `tests/test_cainos_tileset.gd`** (15 pins, all green locally, Godot
  4.3.stable): peering-bits-present bug-class guard (path-fill cell all-path
  corners, grass-field all-grass, edge cell is a real mixed-terrain transition,
  ≥1 transition tile exists), terrain-set config (corner mode, grass+stone_path),
  6-source count, scene + palette load smoke, Ground uses Cainos set, starter patch
  painted. `NoWarningGuard` attached (universal warning gate).
- **`cainos-authoring-scene-soak.spec.ts`** — in-game capture spec, `test.skip`
  CI-inert, diag-build-gated (boots `s1_yard_authored.tscn` via the
  `diag/cainos-authored-soak` branch with `main_scene` swapped, per
  `html5-export.md` § Diagnostic-build pattern). Captures the live HTML5 canvas
  rendering the Cainos tiles — NEVER an offline render tool (`art-direction.md`
  FINAL-pivot "verify against the running game").

## OOS (honored)

- No designed/painted level layout (Sponsor's job).
- Did not touch the held #426.
- Did not rebuild the old procedural `S1YardChunk` ground.

## Cross-lane integration check

- No combat / mob / `[combat-trace]` code touched — contract preserved (no edits to
  `scripts/mobs/*`, `scripts/combat/*`, `scripts/player/Player.gd`).
- No player iframes / Damage-formula constants touched.
- No RoomGate / signal-chain edits.
- `Main.tscn` `main_scene` unchanged on the feature branch (only the throwaway diag
  branch swaps it). The default game boot is byte-identical.
- New scene reuses the existing `scenes/player/Player.tscn` instance (autoload deps
  satisfied by project.godot autoloads) + standard `TileMapLayer` convention from
  the existing chunk scenes.

## Doc updates

Candidate maintain-docs finding flagged in the final report (Cainos paintable-setup
conventions: API-built terrain tileset + paintable authoring-scene pattern + diag-
build-for-authoring-scene-capture). No `.claude/docs/` edits made in this PR.
